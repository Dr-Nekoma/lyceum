[
    {kernel, [
        {logger, [
            %% Console logger
            {handler, structured, logger_std_h, #{
                formatter =>
                    {flatlog, #{
                        map_depth => 3,
                        term_depth => 50
                    }}
            }},
            {log_level, info}
        ]}
    ]}
].
